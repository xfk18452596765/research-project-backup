# 09 基线就绪结论

## 可复现来源

- base commit: `30b55a8af5b307feabc94b7e419e27879a511908`
- ns-3 version: 3.43
- official tag commit: `753817468d611239b1e3c2e272b2bed8ef1f580c`
- clean source archive SHA256:
  `da33eb5f3abb5e304ac8664f66ff117974be9811daa7cd340753e080902673fc`
- patch SHA256:
  `90823ff0d2bc380ad838007293b87c87f3631c73b313b38b92d4da46907184db`
- overlay SHA256:
  `728e96947078437923f716293e10186371d775fc826215e0f3e22bea9348fcd3`
- patched source SHA256:
  `feb05e33d6d3a34fa3016e7ceb02ea3bb9ad0c5d8e037c39df652b23e9690067`

## Gate 0—6

- Gate 0 build: PASS。完整 build 952/952；官方 QUICK 768/768，
  0 failed、0 crashed；scratch 最终目标增量重编成功。
- Gate 1 topology: PASS。相邻逐对 probe 可解码，非相邻不能直接解码；
  RSS、CCA 与位置矩阵已保存。隐藏终端产生 70 条 PHY/MAC drop 证据。
- Gate 2 DCF causality: PASS。1/2/4/6-hop 单包全部交付；18 个低负载
  case 共 180/180；逐包 receive-before-forward，无 L2 捷径。
- Gate 3 K2 lifecycle: PASS。2/4/6-hop 分别 1/2/3 段；PR_ACK 后 DATA，
  RELEASE 完成后下一段。冲突场景实际发送 PR_NACK，首重试 CW=31，
  fresh attempt 后交付。
- Gate 4 reservation medium effect: PASS。Txop trace 证明 reserved 帧不再
  逐 hop 选择随机退避；本地普通接入有 block；多流中存在距离至少三节点的
  重叠 grant，证明不是全局锁；RELEASE 清理后恢复。
- Gate 5 loss accounting: PASS。60 个结果全部满足
  `created=sum(terminal_counts)`、`UNKNOWN_LOSS=0`、无遗留 active grant。
- Gate 6 smoke: PASS。48 个规定的低/高负载 case 均终止、可解析且损失可解释。

## 其他必需证据

- 多流 M1：两个独立 flow，10/10 包交付。
- 多流 M2：两个反向 flow，9/10 包交付；余包为
  `SIMULATION_STOP_TIMEOUT`，本阶段无性能门槛。
- 隐藏终端：A/C 不互相载波感知，A/C 均可到达 B；场景真实运行并终止。
- Day03—Day17 regression：全部通过；以
  `PYTHONDONTWRITEBYTECODE=1` 运行，未修改冻结目录。
- 历史证据：两个 PreDay18 目录 start/end 文件数和 manifest SHA256 完全一致。

## 已知限制

本实现是最小语义基线，不是完整标准化 PRMAC。高负载固定截止时间会产生已分类
的 `SIMULATION_STOP_TIMEOUT`。结果不支持、也没有用于宣称 Fixed 优于 DCF。
后续性能结论必须在独立的 `PreDay18_语义正确基线止损复验` 中产生。

## 最终决定

`BASELINE_READY`

语义正确 ns-3 基线已具备，允许新建独立任务执行
`PreDay18_语义正确基线止损复验`。Day18 继续锁定。

本阶段未重新执行完整止损复验，未重新判定 PreDay18 PASS，未开始 Day18，
未运行 RL。
