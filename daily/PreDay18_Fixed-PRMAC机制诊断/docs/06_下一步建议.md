# 下一步建议

下一阶段只能继续 Fixed-PRMAC 机制诊断，不能进入 Day18 或 RL。

建议顺序：

1. 先修复仿真实现语义：真实位置/干扰拓扑、接收触发的逐跳转发、K=2 分段、
   帧长预算、冻结 BEB 公式以及逐包 MAC retry trace。
2. 将预约下沉为真正影响介质接入的 MAC 状态/保护窗口；应用层错峰不能代表预约。
3. 分别实现可验证的多流与隐藏终端拓扑，禁止继续用未使用的 `flows` 参数或
   Bernoulli 丢失替代。
4. 在新的独立止损任务中，以修复后的 DCF 与 Fixed 实现重新冻结标准；
   在此之前不得重新判定 PASS。

不能根据本诊断直接宣布协议可用。组合参考仍显示 Fixed 相对 DCF 的交付率损失，
公平性、预约冲突、真实 hidden terminal 和多流行为仍未验证。
