# 机制解释与局限

语义正确 ns-3 中的 Fixed 路线需要逐段 PR_REQ/PR_ACK、DATA/H_ACK 与
RELEASE 生命周期。正式负载下，额外控制往返与本地预约阻塞没有转化为端到端
优势，反而出现显著排队、超时和交付损失。

控制丢失实验对 PacketSocket 上实现的逻辑控制帧执行可重放注入；DCF 对照保留
原生 802.11 ACK/重试。Python 是离散事件抽象，不要求与 ns-3 绝对数值一致。
