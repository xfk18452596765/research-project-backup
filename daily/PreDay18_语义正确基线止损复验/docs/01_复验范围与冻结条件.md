# 复验范围与冻结条件

比较对象仅为 DCF 与 Fixed-PRMAC。K=2、CWmin=15、CWmax=1023、
retry limit=7；Bootstrap 10,000 次，分析种子 20260730。判定策略在正式矩阵前
冻结，SHA256 见 `results/audit/stop_loss_policy_sha256.json`。未运行 RL，
未创建或修改 Day18。
