# V2 implementation and build decision

`SEMANTIC_BASELINE_V2_HOLD`. Official clean ns-3.43 A/B sources were acquired.
The new overlay is an executable DCF chain smoke program and is retained as
source. The essential Fixed-PRMAC V2 local reservation mechanism has not been
implemented in `Txop`, `ChannelAccessManager`, `FrameExchangeManager`, or an
equivalent local Wi-Fi access component. Consequently no claims are made for
Gate 5--7, patch equivalence, A/B build, official tests, or smoke matrices.
The minimal next repair is to implement and wire a per-node local reservation
gate into the Wi-Fi MAC access path, then regenerate and validate the patch.
