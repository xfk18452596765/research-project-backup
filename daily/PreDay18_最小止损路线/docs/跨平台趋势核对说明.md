# 跨平台趋势核对

核对 periodic/poisson 的 4-medium、4-high、6-medium、6-high。比较 Fixed−DCF 平均时延符号、交付率差值、随 hop/load 的变化以及控制字节开销。自动结果位于 `results/cross_platform/`。

一致方向可作为交叉证据，不一致方向或能力缺口只能导致 HOLD/FAIL，不能通过调整 K、CW、种子或门槛消除。
